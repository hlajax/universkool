<%= render 'menus/main' %>
<div class="container">
	<div class="row">
		<div class="col-lg-12 col-12 bg-white m-top">
			    <div class="row">
			    	   <div class="col-lg-9 col-12 m-top">
	                         <h1 class="text-bold text-cap"><%= @article.titre %></h1>
		                      <div><%= image_tag @article.photo.url, class:"img-fluid m-top" %></div>
                              <p><%= raw(@article.contenu) %></p>
		               </div>
			    </div>
		</div>
	</div>
</div>
<%if administrateur_signed_in?%>
<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
<% else %>
<% end %>
<%=render 'footers/main-footer'%>